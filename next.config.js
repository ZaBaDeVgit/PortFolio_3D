/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/PortFolio_3D',
  images: {
    unoptimized: true,
  },
  trailingSlash: true,
};

module.exports = nextConfig;
